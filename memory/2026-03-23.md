# 2026-03-23

- 语音项目当前状态：未完成可交付版本；已切换技术路线为 `openWakeWord`，目标是实现 "hey jarvis" 唤醒 + 语音输入 + LLM 回复 + 语音播报的完整链路。
- 已确认并保留脚本文件：`C:\Users\itach\.openclaw\workspace\jarvis_ow.py`。
- 运行环境关键约束：Windows ARM64 + Python 3.12；`pyaudio` 编译失败，已放弃，改用 `sounddevice`；`edge-tts` 因 `aiohttp` 构建失败不可用，后续需改走 Windows 内置 TTS 或其他免编译方案。
- OpenClaw Gateway 本地端口 `127.0.0.1:18789` 可连通，但 `POST /v1/chat/completions` 在当前环境下持续读超时，尚未打通为稳定可调用链路。
- 通过 `openclaw agent --agent main` 实测发现：Gateway 连接会回落到 embedded；`openai-codex` OAuth 刷新失败，报错 `unsupported_country_region_territory`，随后模型 fallback 到 `minimax-portal/MiniMax-M2.7` 成功。
- 因此，当前对话/代理链路可用模型实际回退到 `MiniMax-M2.7`，但 HTTP chat completions 方案仍不可依赖。
- 对用户的明确反馈：项目“代码骨架已搭好，但还不能交付使用”；下一步应改为更稳的本地链路，优先追求能跑的最小可用版，而不是继续卡在 OpenClaw HTTP 接口。
- 新确认的可用技术结论：`openWakeWord` 已成功下载并加载 `hey jarvis` 预训练模型，且需显式使用 `inference_framework='onnx'` 才能绕过缺失的 `tflite-runtime`。
- 新确认的本地能力：`sounddevice` 已实测可录制 16k 单声道音频；`pyttsx3` 可正常播报，系统存在中文语音 `Microsoft Huihui Desktop - Chinese (Simplified)`。
- 新确认的 LLM 备用链路：直接调用配置中的 `https://api.minimaxi.com/anthropic/v1/messages` 已返回 HTTP 200，可作为 `jarvis_ow.py` 的稳定对话后端，替代不可用的 OpenClaw `chat/completions` HTTP 接口。
- 当前剩余主阻塞：STT 还未闭环；`speech_recognition` 已安装，但麦克风枚举依赖 `pyaudio` 不可用，因此后续应采用 `sounddevice` 录音后自行封装音频，再接可用识别接口或本地识别方案。
- 用户对语音项目结果强烈不满，要求回滚并清除语音项目所有内容，包括安装的工具和项目生成的文件。
- 已执行的回滚：删除 `C:\Users\itach\.openclaw\workspace\jarvis_ow.py`；卸载 `openwakeword`、`sounddevice`、`pyttsx3`、`SpeechRecognition`；确认 `edge-tts` 与 `pyaudio` 原本就未安装成功。
- 当前回滚范围只覆盖本轮明确安装/生成的内容；是否继续清除历史 Jarvis 文件（如 `jarvis_final.py`、`jarvis.html`、`启动语音助手.bat` 与旧 9876 方案相关文件）仍待用户进一步确认。
- 用户随后明确要求“全部清掉”；已继续执行更彻底但仍谨慎限定范围的清理。
- 继续清理结果：删除 `C:\Users\itach\AppData\Roaming\Microsoft\Windows\Recent\voice_assistant.lnk`；再次确认 Python site-packages 中无 `openwakeword`、`sounddevice`、`pyttsx3`、`speech_recognition` 等残留。
- 后续针对 `jarvis_final.py`、`jarvis.html`、`启动语音助手.bat`、文件名含 `jarvis`、以及内容含 `127.0.0.1:9876` / `openwakeword` / `hey jarvis` 的项目文件做了定向搜索；未发现可明确归属本次语音项目且适合继续删除的目标。
- 最终对用户的确认结论：本轮语音项目能明确识别和安全删除的内容已经全部清除；未删除 OpenClaw 自带 `voice*` 文档、其他独立项目（如 `kiwi-voice`）及系统/应用通用 voice 相关文件，以避免误伤。
- 新一轮工作方向已切换为“嬴 AgentOS v2 升级蓝图”：不再只谈主观改进，而是基于外部最佳实践与热门 Agent 项目，规划一个稳定、安全、可观测、可分工、可进化的 Agent 操作系统。
- 已完成外部调研并形成关键共识：优先采用 Anthropic 提倡的 workflow 优先于过度自治思路，并吸收 OpenAI 关于 built-in tools、orchestration、observability 的产品方向，以及 `bytedance/deer-flow`、`langchain-ai/open-swe`、`volcengine/OpenViking`、`obra/superpowers` 等项目在 subagents、memory、sandbox、skills、trace、handoff 上的做法。
- 已输出正式蓝图文件：`C:\Users\itach\.openclaw\workspace\docs\plans\2026-03-23-agentos-v2-blueprint.md`；该蓝图采用六层架构（治理层、编排层、执行层、上下文层、工具/环境层、观测评估层）和三阶段实施路线（Phase 1 止血治理、Phase 2 多 Agent 编排、Phase 3 工具/上下文/观测闭环）。
- 对用户的明确建议已收敛为：优先从 Phase 1 开始落地，即先建立任务分级、执行闸门、失败分类、稳定性 guardrails，再进入多 Agent 与可观测升级，避免再次陷入“边做边乱试”的模式。
- 用户明确提出为 AgentOS v2 加上“记忆分层索引配合”，目标是让记忆既尽量完整，又避免每次读取过多内容；该方向已被判断为 v2 的关键基础设施，而非可选优化项。
- 已收敛出的记忆架构建议为多层索引树：L0 总索引层、L1 领域索引层、L2 主题摘要层、L3 详细事实层、L4 原始证据层，并可再叠加热/温/冷分区，以支持“先索引、再命中、再逐层下钻”的读取方式。
- 已向用户明确建议的读取策略是：简单任务读 L0-L1，普通项目任务读 L0-L2，复杂执行/排障再下钻到 L3，审计或争议核对时才访问 L4；同时采用 daily log、主题摘要、总索引三者并存的双写/分层归档策略，避免信息只留在每日记录里却无法复用。
- 已向用户建议的后续落地方案是先做轻量版：新增 `memory/当前活跃任务.md`、`memory/技术配置.md`、`memory/项目经验.md`、`memory/经验教训.md` 作为索引/摘要层，再逐步扩展到 `memory/projects/`、`memory/decisions/`、热温冷分区与正式读取规则。
- 用户已进一步要求：把这套记忆系统正式并入 `嬴 AgentOS v2` 升级蓝图、实际实现出来并完成测试；这意味着后续工作不再停留在概念设计，而要进入“蓝图更新 → 文件落地 → 读取规则实现 → 验收测试”的执行阶段。
- 当前判断的最小可交付实现范围应包含：更新蓝图以纳入记忆分层架构；创建或补齐索引/摘要文件（至少 `memory/当前活跃任务.md`、`memory/技术配置.md`、`memory/项目经验.md`、`memory/经验教训.md`）；定义默认读取规则；并通过一次真实加载/检索场景验证“减少读取量但不丢关键上下文”的效果。
- 用户已明确确认采用 B 路线：测试通过后，将该方案封装为“可独立复用的正式 skill”，并进一步要求最终放入 GitHub 仓库开源。
- 当前已识别的发布前置条件是：必须先完成实现与测试，再确定 GitHub 仓库落点；当前 workspace 虽是 git 仓库，但尚未配置可用 remote，因此后续还需要用户确认是新建独立仓库还是指定现有 `owner/repo`。
- 用户后来已明确拍板继续执行，不再要求中途停下来等确认；之后再因 checkpoint 或 compaction 而中断推进，属于执行失误，不应再重复发生。
- GitHub 仓库名已由用户明确指定为 `layered-memory-skill`；未来开源发布时，README 必须按正式规范完整撰写，而不是只放最小说明。
- 已实际落地 Phase 1 的第一批核心治理文件：`knowledge/task-grading.md`、`knowledge/execution-gates-v2.md`、`knowledge/failure-taxonomy.md`、`knowledge/stability-guardrails-v2.md`；内容已覆盖任务分级、执行闸门、失败分类、稳定性守则四个基础面。
- 这批文件当前已写入工作区并完成文件级存在校验，但尚未继续推进到后续批次（如 `knowledge/context-loading-model.md`、skill 封装、README、测试用例与 Git 提交）。
- 用户在 session 恢复后说「继续完成它」，我按 `executing-plans` skill 继续推进第二批的执行。
- 第二批计划内容：`knowledge/context-loading-model.md`、更新蓝图、创建 `skills/layered-memory-skill/` 初版目录、写 README、测试用例。
- 第二批执行期间，workspace 有大量未追踪文件（AGENTS.md、BOOTSTRAP.md、MiroFish/、RLG 相关文档等），与本阶段任务无关，暂不处理。
- 当前阶段整体状态：Phase 1 第一批完成，等待继续推进第二批。
- 第二批执行内容：`knowledge/context-loading-model.md`（已补 L4 实现）、`skills/layered-memory-skill/` 完整结构、`evidence/` L4 存储目录（`conversations/`、`tool-outputs/`、`external-fetches/`）、`evidence/L4-implementation.md`。
- 测试与评估结果：L0-L3 分层体系可正常工作；L4 发现缺口（无明确存储结构）；已补 L4 实现方案，缺口已修复。
- 测试结论：系统"够用但不完美"；L4 原始证据层已补全，三类存储目录与格式模板已定义。
- 第二批文件已部分 commit（governance files + skill scaffold），L4 相关新增文件待 commit。
