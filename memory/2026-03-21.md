# 2026-03-21 日记

## 今天做了什么

### 1. 每日简报 Cron 执行
- 尝试执行 AI 新闻简报任务
- 成功获取了 The Verge / VentureBeat / GitHub Trending 数据
- 飞书推送失败（OAuth 未授权）
- 内容已整理但无法发送

### 2. Claude Code 配置
- 彭科提供了中转 API（claudecodeserver.top）
- API Key 格式是 `sk_`，被服务器要求 `sk-ant-` 格式
- 实际测试发现：`allowedClients: ["claude_code"]` — 该服务只接受 Claude Code CLI
- **结论**：Claude Code CLI 本身正常工作（`claude -p "say hi"` ✅）
- **结论**：无法通过普通 HTTP 请求接入 OpenClaw 模型

### 3. 双层记忆体系重构
- 创建了 `memory/` 下的5个子文件：
  - `关于彭科.md`
  - `技术配置.md`
  - `项目经验.md`
  - `经验教训.md`
  - `能力库.md`
- 重写了 `MEMORY.md` 作为入口索引
- 从此主文件保持轻量，详细内容分布到子文件

### 4. OpenClaw 模型配置
- 添加了 `claudecode` provider（但实际不可用，因为中转只认 Claude Code CLI）
- 验证了中转 API 的真实限制

## 重要发现

- **claudecodeserver.top 的限制**：只接受 `claude_code` 客户端，请求头需要特定标识
- 这意味着：无法通过 OpenClaw 的 HTTP 请求使用这个中转 API
- 只能通过 Claude Code CLI 使用，或找其他中转服务

## 彭科的状态

- 配置 Claude Code 成功，很满意
- 要求把 Claude Code 接入 OpenClaw → 已分析不可行原因
- 开始思考双层记忆体系 → 已实现

## 明天要做什么

- [ ] 写个更完整的关于彭科档案（沟通风格细节）
- [ ] 等彭科确认 Claude Code 接入 OpenClaw 的后续方案
- [ ] 继续优化语音助手唤醒可靠性
